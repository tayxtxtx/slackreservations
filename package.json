{
  "name": "autobay-reservations-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Slack bot for Autobay reservations backed by Google Calendar",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "check": "node --check src/app.js && node --check src/botLogic.js && node --check src/calendarService.js && node --check src/configuration.js"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "botkit": "^0.7.4",
    "googleapis": "^150.0.1"
  }
}
